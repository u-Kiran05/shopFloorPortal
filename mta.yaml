_schema-version: "3.2"
ID: shopFloor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: shopFloor-destination-content
  type: com.sap.application.content
  requires:
  - name: shopFloor-destination-service
    parameters:
      content-target: true
  - name: shopFloor-repo-host
    parameters:
      service-key:
        name: shopFloor-repo-host-key
  - name: shopFloor-uaa
    parameters:
      service-key:
        name: shopFloor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: shopFloor_html_repo_host
          ServiceInstanceName: shopFloor-html5-service
          ServiceKeyName: shopFloor-repo-host-key
          sap.cloud.service: shopFloor
        - Authentication: OAuth2UserTokenExchange
          Name: shopFloor_uaa
          ServiceInstanceName: shopFloor-xsuaa-service
          ServiceKeyName: shopFloor-uaa-key
          sap.cloud.service: shopFloor
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: shopFloor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: shopFloor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - shopFloor.zip
      name: shopFloor
      target-path: resources/
- name: shopFloor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: shopFloor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: shopFloor-destination-service
    service-plan: lite
- name: shopFloor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: shopFloor-xsuaa-service
    service-plan: application
- name: shopFloor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: shopFloor-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
